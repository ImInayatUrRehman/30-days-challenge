// write a script to calculate the sum of number from 1 to 10 using while loop;
{
    var num = 1;
    var sum = 0;
    while (num <= 10) {
        sum += num;
        num++;
    }
    console.log(sum);
}
